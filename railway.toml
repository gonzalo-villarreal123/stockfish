[build]
dockerfilePath = "apps/agents/Dockerfile"
buildContext = "apps/agents"

[deploy]
startCommand = "uvicorn api:app --host 0.0.0.0 --port 8000"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
